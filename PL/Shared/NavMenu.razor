


<MudDrawer @bind-Open="@open" ClipMode="clipMode" Elevation="0" Variant="@DrawerVariant.Responsive" Anchor="Anchor.Right" PreserveOpenState="true" Color="Color.Surface">
       @*  <MudDrawerHeader>
        </MudDrawerHeader>
     *@
    <MudNavMenu Style="font-family:dana" Color="Color.Surface">
        <MudText Class="my-4 px-4" Typo="Typo.h6" Style="font-family:dana">دستیار کامکو</MudText>
        <MudDivider Class="d-inline-flex" />

        <MudNavLink Icon="@Icons.Material.Filled.Home">صفحه اصلی</MudNavLink>
        <MudNavGroup Class="link-dark" Icon="@Icons.Material.Filled.People" Title="پرسنل">

        </MudNavGroup>
        <MudNavLink>Library</MudNavLink>
        <MudNavLink>Community</MudNavLink>
    </MudNavMenu>
</MudDrawer>






@code {

    bool open = true;
    bool dense = true;
    DrawerClipMode clipMode = DrawerClipMode.Always;
    public void ToggleDrawer()
    {
        open = !open;
        Console.WriteLine(open);
        StateHasChanged();
    }
}
